#include <iostream>
#include <math.h>
#include <stdio.h>

enum commands 
    {
    HALT = 0,
    PUSH = 1,
    POP  = 2,
    ADD  = 3,
    MUL  = 4,
    JMP  = 5,
    PRNT = 6,
    IN   = 7,
    CALL = 8,
    RET  = 9,
    JA   = 10,
    JAE  = 11,
    JB   = 12,
    JBE  = 13,
    JE   = 14, 
    DIV  = 15,
    SUB  = 16,
    JNE  = 17,
    SQRT = 18
    };
